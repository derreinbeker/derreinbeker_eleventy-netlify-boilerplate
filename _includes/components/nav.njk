<nav>
  <ul>
  {%- for nav in navigationEntries -%}
    <li>
      <a href="{{ nav.url | url }}"{% if nav.url in page.url %} data-current="current item"{% endif %}>{{ nav.title }}</a>
    </li>
  {%- endfor -%}
  </ul>
</nav>
